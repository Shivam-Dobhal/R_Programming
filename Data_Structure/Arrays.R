# creating an array
# arrays function create n dimensional arrays 
# it takes dim(dimension) attributes as an arguments 
# and create required length of each dimension as specified in attribute

ar1<-array(c(1:3),dim=c(3,2,1)) # in dim, there 3 rows, 2 columns and 1 no.of array(quantity of array) 
print(ar1)

a<-0%/%0
print(a)
v66<-c(1,7,9,NA,77,7,99,9)
print(f1<-factor(v66))